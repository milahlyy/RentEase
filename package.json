{
  "name": "rentease",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write .",
    "lint": "cd packages/shared && bun run lint && cd ../db && bun run lint && cd ../../apps/api && bun run lint && cd ../web && bun run lint",
    "seed": "cd packages/db && bun run seed",
    "typecheck": "cd packages/shared && bun run typecheck && cd ../db && bun run typecheck && cd ../../apps/api && bun run typecheck && cd ../web && bun run typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "prettier": "^3.3.3",
    "turbo": "^2.0.12",
    "typescript": "^5.5.3"
  }
}
